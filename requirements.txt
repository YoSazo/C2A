# Most dependencies you already have from C2A-AI
# Only install these if missing:

sentence-transformers>=2.2.0
# You already have: chromadb, ollama, sqlite3, numpy

# Optional (only if you want other LLM providers):
# anthropic>=0.3.0
# openai>=1.0.0  
# google-generativeai>=0.3.0